# Copy this file to .env.dev.local and fill in the values.
# .env.dev.local is in .gitignore by default.

# In prod, define environment variables in your platform.
# In dev, you can use this file to define default values.
# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE.

###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=your-secret-key-here
###< symfony/framework-bundle ###

###> doctrine/doctrine-bundle ###
# Database Configuration
# The setup script will override these with your input.
MYSQL_USER=
MYSQL_PASSWORD=
MYSQL_DATABASE=
MYSQL_ROOT_PASSWORD=
MYSQL_VERSION=8

# Database URL format: "mysql://USER:PASSWORD@HOST:PORT/DATABASE?serverVersion=xx&charset=utf8mb4"
# For local development, use the database service name "database" as the host (Docker internal DNS)
# For accessing from host machine (e.g., database clients), use 127.0.0.1 with DB_HOST_PORT
DATABASE_URL=mysql://${MYSQL_USER}:${MYSQL_PASSWORD}@database:3306/${MYSQL_DATABASE}?serverVersion=${MYSQL_VERSION}&charset=utf8mb4
###< doctrine/doctrine-bundle ###

###> symfony/mailer ###
# Use the "mailer" service name for Docker internal communication
MAILER_DSN=smtp://mailer:1025
###< symfony/mailer ###

###> symfony/mercure-bundle (OPTIONAL) ###
# Uncomment these lines if you install symfony/mercure-bundle
# See https://symfony.com/doc/current/mercure.html#server-sent-events-sse-mercure
# MERCURE_URL=http://php/.well-known/mercure
# MERCURE_PUBLIC_URL=https://localhost/.well-known/mercure
# MERCURE_JWT_SECRET="!ChangeThisMercureHubJWTSecretKey!"
###< symfony/mercure-bundle ###
