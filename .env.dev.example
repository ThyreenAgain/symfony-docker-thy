# Copy this file to .env.dev.local and fill in the values.
# .env.dev.local is in .gitignore by default.

# In prod, define environment variables in your platform.
# In dev, you can use this file to define default values.
# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE.

###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=your-secret-key-here
###< symfony/framework-bundle ###

###> doctrine/doctrine-bundle ###
# Database Configuration
# The setup script will configure these based on your database selection (DB_TYPE).

# MySQL Configuration (used if DB_TYPE=mysql)
MYSQL_USER=
MYSQL_PASSWORD=
MYSQL_DATABASE=
MYSQL_ROOT_PASSWORD=
MYSQL_VERSION=8
MYSQL_CHARSET=utf8mb4

# PostgreSQL Configuration (used if DB_TYPE=postgres or DB_TYPE=postgis)
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

# Database URL - will be set automatically based on DB_TYPE by setup script
# MySQL format:
# DATABASE_URL=mysql://${MYSQL_USER}:${MYSQL_PASSWORD}@database:3306/${MYSQL_DATABASE}?serverVersion=${MYSQL_VERSION}&charset=${MYSQL_CHARSET}
#
# PostgreSQL format:
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@database:5432/${POSTGRES_DB}?serverVersion=18&charset=utf8
#
# PostGIS format (same as PostgreSQL):
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@database:5432/${POSTGRES_DB}?serverVersion=18&charset=utf8
DATABASE_URL=
###< doctrine/doctrine-bundle ###

###> symfony/mailer ###
# Use the "mailer" service name for Docker internal communication
MAILER_DSN=smtp://mailer:1025
###< symfony/mailer ###

###> symfony/mercure-bundle (OPTIONAL) ###
# Uncomment these lines if you install symfony/mercure-bundle
# See https://symfony.com/doc/current/mercure.html#server-sent-events-sse-mercure
# MERCURE_URL=http://php/.well-known/mercure
# MERCURE_PUBLIC_URL=https://localhost/.well-known/mercure
# MERCURE_JWT_SECRET="!ChangeThisMercureHubJWTSecretKey!"
###< symfony/mercure-bundle ###
